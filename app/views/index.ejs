<div class="container">

	<% _.each(items, function(topC) { %>
	<div class="jumbotron">
		<h1><%= topC.name %></h1>
		<p><%= topC.page_description %></p>
		<% if (topC.image) { %>
		<div>
			<img src="/images/<%= topC.image %>">
		</div>
		<% } %>
	</div>
	<% }); %>

</div> <!-- /container -->